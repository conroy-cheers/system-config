diff --git a/python/triton/language/core.py b/python/triton/language/core.py
index 0287c84..a11945b 100644
--- a/python/triton/language/core.py
+++ b/python/triton/language/core.py
@@ -2162,7 +2162,10 @@ def load(pointer, mask=None, other=None, boundary_check=(), padding_option="", c
         other = _semantic.to_tensor(other)
     padding_option = _unwrap_if_constexpr(padding_option)
     cache_modifier = _unwrap_if_constexpr(cache_modifier)
-    eviction_policy = _unwrap_if_constexpr(eviction_policy)
+    # Pascal PTX does not support the cache eviction modifiers emitted by
+    # newer Triton kernels. Keep the semantic parameter accepted, but lower it
+    # to the default policy so kernels can compile for sm60.
+    eviction_policy = _unwrap_if_constexpr("")
     volatile = _unwrap_if_constexpr(volatile)
     return _semantic.load(pointer, mask, other, boundary_check, padding_option, cache_modifier, eviction_policy,
                           volatile)
